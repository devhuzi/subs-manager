# ── Secrets & env ────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
# local-only cron secret scratch
.cron-secret.txt

# ── Local-only Claude Code state ─────────────────────────────────────────────
CLAUDE.local.md
.claude/settings.local.json
.claude/worktrees/
# vendored skill (reinstall: npx impeccable skills install)
.claude/skills/impeccable/

# ── Dependencies & build output ──────────────────────────────────────────────
node_modules/
dist/
dev-dist/
build/*
# Generated by scripts/*.ps1 but required to build/package — keep them tracked.
!build/icon.png
!build/tray-icon.png
release/
src/web/dev-dist/

# ── Caches & tool artifacts ──────────────────────────────────────────────────
.eslintcache
.vite/
*.tsbuildinfo
coverage/

# ── Tests / browser-test harness ─────────────────────────────────────────────
test-results/
playwright-report/
blob-report/
playwright/.cache/
e2e/screenshots/

# ── Local design references (Dribbble inspiration — not needed by the app) ───
design-refs/

# ── Workspace scratch & logs ─────────────────────────────────────────────────
.tmp/
logs/*.log
# debug captures — may contain live auth tokens
*.har

# ── Language artifacts ───────────────────────────────────────────────────────
__pycache__/
*.pyc
*.egg-info/
*.local

# ── Editors / IDEs ───────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Deployment-specific legal pages (host your own; see VITE_PRIVACY_URL) ─────
docs/privacy-policy.md
docs/terms-of-service.md
